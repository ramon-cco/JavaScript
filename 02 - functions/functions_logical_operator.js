/* 
    Operadores lógicos (logical operator)

    2 valores booleanos, quando verificados, resultará em verdadeiro ou falso
*/

let pao = false
let queijo = true

// AND && ( os dois tem que ser verdadeiros)
// console.log(pao && queijo)

// OR || ( um precisa ser verdadeiro)
// console.log(pao || queijo)

// NOT ! ( negação)
console.log(!pao)