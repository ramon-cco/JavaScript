/* 
    Operadores de String (string operator)
*/

// comparison (comparação)
console.log('a' == 'a')

//concatenarion (concatenação)
//retorna a união de duas Strings

let alpha = 'alpha'
console.log( alpha + 'bet' + 's') // or

console.log( alpha += 'bet' + 's') // or

let bravo = 'bra'
bravo += 'us'
console.log(bravo)
