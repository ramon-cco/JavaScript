/*
Estrtura de repetição
    while

    sintax:
    while (condição) {
     rotina
    }
    }

    Exemplo:
    O seguinte laço while itera enquanto n é menor que três.

    var n = 0;
    var x = 0;

    while (n < 3) {
    n++;
    x += n;
    }

*/
// ideal para quando nao sabemos o momento que a execursão terá fim.

let i = 49656587996132
while(i > 10) {
    console.log(i)

    i /=35 // vai dividir o número até que o valor seja menor que 10
}

